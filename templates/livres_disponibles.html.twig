{% extends 'base.html.twig' %}

{% block title %}Livres disponibles{% endblock %}

{% block body %}
<div class="container">
    <h1>📚 Livres disponibles</h1>

    {% if livres is empty %}
        <p>Aucun livre disponible pour le moment.</p>
    {% else %}
        <table>
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Auteur</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for livre in livres %}
                    <tr>
                        <td>{{ livre.titre }}</td>
                        <td>{{ livre.auteur }}</td>
                        <td>
                            <a href="{{ path('reserver_livre', {'id': livre.id}) }}" class="btn">📖 Réserver</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
</div>
{% endblock %}
